name: fawry_payment
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  network:
    path: ../../network
  failures:
    path: ../../failures
  core:
    path: ../../core
  ui_components:
    path: ../../ui_components
  payment_domain:
    path: ../../payment_domain
  domain_models:
    path: ../../domain_models
  localization:
    path: ../../localization
  form_fields:
    path: ../../form_fields

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:

